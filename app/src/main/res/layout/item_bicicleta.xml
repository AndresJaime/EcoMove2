<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tituloBicicletaTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="Bicicleta"
        android:textSize="24sp"
        android:textColor="@android:color/white"/>

    <ImageView
        android:id="@+id/imagenBicicletaImageView"
        android:layout_width="343dp"
        android:layout_height="375dp"
        android:layout_below="@id/tituloBicicletaTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_bike_placeholder" />

    <Button
        android:id="@+id/reservarButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/imagenBicicletaImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@drawable/black_border_button"
        android:text="Reservar"
        android:textColor="@android:color/white"/>

    <TextView
        android:id="@+id/existenciasBicicletaTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/reservarButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="Existencias: "
        android:textSize="18sp"
        android:textColor="@android:color/white"/>
</RelativeLayout>
